/**
 * Custom type definitions for Hyperliquid integration
 */

export interface MarketStats {
  price: string;
  volume24h: string;
  bid: string;
  ask: string;
  mark: string;
  openInterest: string;
  fundingRate: string;
  spread: string;
}
